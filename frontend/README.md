# E‑Commerce Demo App (Medium Complexity)

Full‑stack application with FastAPI backend, React frontend, JWT auth, product/order management, and inventory checks.

## Run locally

### Backend
```bash
cd backend
pip install -r requirements.txt
python run.py
pip install pydantic-settings